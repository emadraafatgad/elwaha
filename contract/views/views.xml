<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="contract.list">
      <field name="name">contract list</field>
      <field name="model">contract.contract</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="contract.action_window">
      <field name="name">contract window</field>
      <field name="res_model">contract.contract</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="contract.action_server">
      <field name="name">contract server</field>
      <field name="model_id" ref="model_contract_contract"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="contract" id="contract.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="contract.menu_1" parent="contract.menu_root"/>
    <menuitem name="Menu 2" id="contract.menu_2" parent="contract.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="contract.menu_1_list" parent="contract.menu_1"
              action="contract.action_window"/>
    <menuitem name="Server to list" id="contract" parent="contract.menu_2"
              action="contract.action_server"/>
    -->
  </data>
</odoo>